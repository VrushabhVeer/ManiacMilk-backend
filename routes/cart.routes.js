import { Router } from "express";
const cartRouter = Router();

// Add or update an item in the cart
cartRouter.post("/add", async (req, res) => {
  try {
    
  } catch (error) {
    
  }
});

// Get cart items
cartRouter.get("/:sessionId", async (req, res) => {
 try {
  
 } catch (error) {
  
 }
});

export default cartRouter;
